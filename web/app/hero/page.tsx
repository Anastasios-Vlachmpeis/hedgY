import { HeroLanding } from "@/components/hero/hero-landing";

export const metadata = { title: "hedgY — Invest in what happens. Hedge what matters." };

export default function HeroPage() {
  return <HeroLanding />;
}
